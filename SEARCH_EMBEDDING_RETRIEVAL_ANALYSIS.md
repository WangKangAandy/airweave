# Airweave Embedding / 检索链路分析

本文基于当前仓库实现，聚焦两个问题：

1. 现有 embedding / retrieval 实现是否存在明显逻辑问题；
2. 为什么在“官方文档类问答”场景里会出现精度不稳定。

## 1) 结论概览

- 当前实现整体是可用且自洽的，未发现“单点致命 bug”导致检索必然错误。
- 但系统默认策略更偏向高召回（recall）而非高精度（precision），在“必须精确命中指定文档/段落”的任务中容易产生偏差。
- 主要风险不在“是否有 embedding”，而在“多策略融合 + 查询扩展 + 排序窗口”带来的排序漂移。

## 2) 代码级观察

### 2.1 索引阶段：dense 与 sparse 的输入来源不一致

- Dense embedding：对 `textual_representation` 做向量化（语义主通道）。
- Sparse embedding：对实体 `model_dump` 后的 JSON 字符串做向量化（关键词通道）。

这意味着两条通道的“文本表示空间”不一致，可能导致融合排序时出现偏移，尤其在文档问答场景下更明显。

## 2.2 检索阶段：Vespa 混合策略是“召回优先”

- 检索子句中 hybrid 会组合 `userInput(@query)`（文本召回）和 `nearestNeighbor(...)`（向量召回）。
- 排序侧又融合了 semantic + sparse 分数。
- 这种设计有利于“不要漏”，但更容易把不够精确但“相关”的候选提上来。

## 2.3 查询扩展：默认会放大召回面

- Query expansion 会生成多个改写 query（例如固定数量的 alternatives）。
- 对 FAQ/开放问答帮助较大；但对“安装步骤必须严格按官方文档”这类任务，会增加语义漂移风险。

## 2.4 双搜索栈并存的认知成本

- 仓库中存在 legacy `airweave/search/` 与新 `airweave/domains/search/` 两套体系。
- 若没有明确当前请求命中了哪条路径（instant/classic/agentic 或 legacy），容易出现“同问题不同结果”的体感差异。

## 3) 为什么会出现“回答不够准确”

从系统行为看，常见是以下组合问题：

1. 命中候选里“通用最佳实践”文档被提前；
2. query expansion 扩大了召回集合；
3. rerank 窗口/特征不足以把“官方安装指南”稳定压到 top；
4. 生成层没有被“强引用约束”绑定，补全了行业通用步骤。

## 4) 改进建议（按优先级）

### P0（先做）

- 对“安装/运维/官方规范”类请求默认关闭 query expansion。
- 增加“必须引用来源”的回答约束：无引用不输出结论。
- 检索前先做 metadata 过滤（产品名、版本、文档类型）再向量检索。

### P1（建议做）

- 统一 sparse 与 dense 的输入语料（至少在文档场景下采用同源文本）。
- 调整 hybrid 融合权重与 rerank 窗口，给权威文档更高稳定性。
- 在服务端记录“top-k 命中文档 + 最终引用文档”，做离线误差分析。

### P2（中期）

- 建立小规模 gold set（10~30 条），持续回归“官方步骤题”精度。
- 在 agent 层加入“先检索文档 ID，再生成”的两阶段协议，减少凭经验补全。

## 5) 当前分支检查：musa-knowledge-mcp 目录

对当前仓库已存在的本地与远端分支进行检查（含 `git fetch --all --prune` 后）：

- 未在任何可见分支中找到 `musa-knowledge-mcp` 目录。

若该目录应当存在，需进一步确认：

- 是否在其他 remote（非 origin）；
- 是否在尚未 fetch 的私有分支；
- 是否为子模块或外部仓库路径映射。

